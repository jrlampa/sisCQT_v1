# Copie para `.env` e ajuste os valores para DEV.
# Nunca versione `.env` com segredos reais.

# Database (Docker Compose)
POSTGRES_USER=admin
POSTGRES_PASSWORD=password123
POSTGRES_DB=siscqt_db

# AI (Gemini)
GEMINI_API_KEY=

# Opcional (dev)
ENABLE_MOCK_AUTH=true

# Desktop (SQLite local)
# Ex.: DESKTOP_DATABASE_URL="file:./.data/siscqt_desktop.db"
DESKTOP_DATABASE_URL=

